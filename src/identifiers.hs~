module identifiers (
             IMSI (..)
                   )
       where

--generateIMSI :: 
--generateIMSI

data IMSI = IMSI {
  mcc :: !Int,
  mnc :: !Int,
  msin :: !Int
  }

instance Show IMSI
         where
           show = show mcc + show mnc + show msin
